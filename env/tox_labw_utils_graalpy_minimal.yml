name: tox_labw_utils_pypy_minimal
channels:
  - nodefaults
  - conda-forge
  - anaconda
  - bioconda
  - main
  - free
  - pytorch

dependencies:
  # Core libraries
  - graalpy-graalvm
  - numpy
  - pandas
  - joblib
  - tomli-w
  - tqdm
  - pytorch=*=py*_cpu_0

  # Testing
  - pytest
  - pytest-cov
  - pytest-html
  - coverage
